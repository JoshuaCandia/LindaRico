import CarouselHeader from '../Carousel/Carousel'

const Header = () => {
  return (
    <header className='h-[90vh] bg-cover bg-center flex flex-col justify-between items-center '>
      <CarouselHeader />
    </header>
  )
}

export default Header
